apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: eece-system
spec:
  accessModes:
    - ReadWriteOnce  # Adjust based on your storage needs
  resources:
    requests:
      storage: 500Mi 